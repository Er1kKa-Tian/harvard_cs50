#include <stdio.h> //This means "standard io", DO NOT TYPE studio!
// This is a "header file"
// Its function is to include the library "stdio.h" which contains the "printf" function

int main(void)
{
    printf("hello, world\n"); //"f" means formatted; double quotes are used for strings; "\n" is called an escape sequence which starts a new line (without that, "$" will appear next to our texts)
    //Also remember to put a semicolumn at the end of a statement
}