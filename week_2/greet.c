#include <cs50.h>
#include <stdio.h>

int main(int argc, string argv[]) // take command line arguments: argc = number of arguments, argv[] = array of arguments
// This is the other common way to define the "main" function
{
    // argv[0] is the command you typed in!
}